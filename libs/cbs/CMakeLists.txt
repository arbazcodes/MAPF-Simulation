file(GLOB_RECURSE HEADERS "include/*.h" "include/*.hpp")
file(GLOB_RECURSE SOURCES "src/*.cpp")
add_library(cbs ${HEADERS} ${SOURCES})
target_include_directories(cbs PUBLIC include)
target_link_libraries(cbs PRIVATE astar)